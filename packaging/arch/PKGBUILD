# Maintainer: Iman <zolfaghari19@gmail.com>
pkgname=nautilus-paste-shortcut
pkgver=0.1.21
pkgrel=1
pkgdesc="Add Paste Shortcut action to GNOME Files"
arch=('any')
url="https://github.com/imz87/nautilus-paste-shortcut"
license=('MIT')
depends=('nautilus' 'python-gobject' 'gtk4' 'python-nautilus')
source=("$pkgname-$pkgver.tar.gz")
sha256sums=('SKIP')

package() {
    cd "$pkgname-$pkgver"
    install -Dm644 src/nautilus_paste_shortcut.py "$pkgdir/usr/share/nautilus-python/extensions/nautilus_paste_shortcut.py"
    install -Dm644 src/core_logic.py "$pkgdir/usr/share/nautilus-python/extensions/core_logic.py"
    install -Dm644 LICENSE "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
    install -Dm644 README.md "$pkgdir/usr/share/doc/$pkgname/README.md"
}

post_install() {
    echo ""
    echo "Restart Nautilus / GNOME Files for the extension to take effect:"
    echo ""
    echo "  nautilus -q"
    echo ""
    echo "Then reopen Files. The \"Paste Shortcut\" menu will appear in the context menu."
    echo ""
}
